<%= render 'pages/menu' %>
<div class="container">
  <p id="notice"><%= notice %></p>
  <h1>Funcion&aacute;rios</h1>

  <table class="table table-sm table-stripped table-bordered">
    <thead>
      <tr>
        <th>E-mail</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @employees.each do |employee| %>
        <tr>
          <td><%= employee.email %></td>
          <td><%= link_to 'Exibir', employee %></td>
          <td><a class="btn btn-info" href="<%= edit_employee_path(employee) %>">Editar</a></td>
          <td><%= link_to 'Apagar', employee, method: :delete, data: { confirm: 'Tem certeza que deseja apagar?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'Novo Funcionário', new_employee_path %>
</div>